# 哎一把维护约定

## 开始任务

1. 阅读 `README.md`、本文件、`package.json`、相关文档与实现。
2. 查看 Git 状态、当前分支、近期提交和远端分支，保护已有修改。
3. 使用 `rg` 查找现有组件、工具函数、测试和相似功能，禁止为同一能力建立平行实现。
4. 先判断任务属于只读巡检、数据修正、前端样式或文案、游戏逻辑、多人协议还是部署运维。

## 架构边界

- 经典推理、单人线索阶梯和时光机在浏览器本地抽题、判定、恢复和结算，不得重新依赖服务端 API。
- 多人模式通过 WebSocket 由服务端统一保管答案、阶段状态、重连和胜负结果。
- 标准题库与扩展题库共用现有数据结构、搜索归一化、拼音与别名匹配。
- 线索字段顺序以 `app/clue-rules.mjs` 为唯一实现；多人服务通过重新导出复用它。
- 优先复用现有题库选择、规则弹窗、本地题袋、战绩、分享图和多人房间实现。

## 验证

- 文案或普通 CSS：类型检查、构建和必要的桌面/移动端查看，不新增独立回归测试。
- 数据：数据校验、数量同步和涉及玩法的核心测试。
- 单人逻辑：本地状态机与相关核心测试。
- 多人逻辑：协议、房间、重连和答案隔离测试。
- 正式发版：`npm run verify:release`；日常提交：`npm run verify`。
- E2E 启动的本地服务必须由测试脚本清理；任务结束前确认没有残留进程或端口。

## 发布与资料隔离

- 公开仓库不得包含生产 IP、SSH 配置、真实服务器目录、证书、凭据、访问日志、恢复记录或内部反代拓扑。
- 生产发布脚本、配置和发布清单只保存在独立私有运维仓库。
- 本地修改、提交、推送和公网部署是四个独立状态，必须分别验证和报告。
- 仅在任务明确要求上线时部署；多人仍有房间或连接时不得中断服务。
- 只读巡检不修改代码、配置或数据，不重启、清理或部署。
