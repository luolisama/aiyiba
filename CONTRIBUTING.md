# 参与贡献

感谢你愿意帮助完善“哎一把”。提交前请先确认改动属于程序修复、题库纠错、文档改进或能够清楚说明规则的
新玩法建议。

## 本地准备

需要 Node.js 22.13 或更高版本。

~~~powershell
npm install
npm run dev
~~~

多人功能需要另开终端运行：

~~~powershell
npm run pk:server
~~~

## 提交代码

1. 从最新 main 创建分支。
2. 每个 Pull Request 只处理一个明确主题。
3. 不要提交 .env、证书、私钥、服务器地址、SSH 配置或生产日志。
4. 不要为一次性的文案、颜色或像素调整新增永久回归测试。
5. 会影响公共状态机、判定、题库结构或安全边界的改动必须补充相应测试。

提交前运行：

~~~powershell
npm run verify
~~~

涉及完整页面流程或多人协议时，再运行 `npm run test:e2e`；正式发版运行 `npm run verify:release`。

## 题库纠错

请优先使用“数据纠错”Issue 表单，并提供作品名与 BV/AV 号、当前错误字段和建议值，以及 Bilibili 投稿页、
作者说明、可信 Wiki 或可复核快照。播放量是时间快照，相关纠错请同时注明核对日期。

## Pull Request

PR 描述需要说明改了什么、为什么改、用户会看到什么变化，以及运行过哪些检查。维护者可能要求拆分与当前
主题无关的改动。
